info:
  name: test-api
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/employees1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
